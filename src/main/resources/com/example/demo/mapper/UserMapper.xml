<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserMapper">
	<select id="selectAllUser"
		resultType="com.example.demo.model.entity.User">
		SELECT
		user_id as userId,
		user_name as userName,
		email,
		role
		FROM users
		ORDER BY user_id
	</select>
	<select id="selectUserByEmail"
		resultType="com.example.demo.model.entity.User">
		SELECT
		user_id as userId,
		user_name as userName,
		email,
		role
		FROM users
		WHERE email = #{email}
	</select>
	<select id="selectUserById"
		resultType="com.example.demo.model.entity.User">
		SELECT
		user_id as userId,
		user_name as userName,
		email,
		role
		FROM users
		WHERE user_Id = #{userId}
	</select>
	<insert id="saveUser"
		parameterType="com.example.demo.model.entity.User"
		useGeneratedKeys="true" keyProperty="userId"
		>
		INSERT INTO users(
			user_name,
			email,
			role
		)
		VALUES(
			#{userName},
			#{email},
			#{role}
		)
	</insert>
</mapper>